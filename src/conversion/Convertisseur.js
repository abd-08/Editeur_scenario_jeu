//import React from "react";

//import * as Constants from "../configuration_app/Constants";

/*
 *
 * Convertit la structure de la base de données en
 * structure utilisable pour l'application et inversement.
 *
 */

/* convertit un objet en json */
export function toJSON (obj) {
    //const objStr = JSON.stringify(obj);
    //return objStr;
}

/* convertit un json en objet */
export function fromJSON (json) {
    //const newObj = JSON.parse(json);
    //return newObj;
}

/* sauvegarde le json dans un fichier  */
export function savingData (json, file) {

}

/* recupere le json depuis un fichier */
export function getData (file) {

}

/*  */